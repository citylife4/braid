# Node
node_modules/
npm-debug.log*
dist/
build/icon.ico
build/icon.png

# Packet engine: downloaded + checksum-verified on demand by
# engine/enable-capture.ps1, never checked in.
engine/tun2socks.exe
engine/wintun.dll

# Capture engine runtime logs + status
engine/capture.out.log
engine/capture.err.log
engine/capture.result.json
